# Sub2API

Sub2API 把客户端请求转成一次或多次上游尝试，并按使用记录计量。本文件只定义领域语言；实现细节见 `docs/adr/`。

## 请求与计量

**逻辑请求**：
客户端对 Sub2API 的一次调用。一次逻辑请求可以对应多次上游尝试。
_Avoid_: 完整请求, 原始请求, 一次上游调用

**上游尝试**：
逻辑请求中实际发往上游的一次请求，包含该次的账号、模型、协议形态和 wire 内容。重试、账号切换、模型改写各自构成新的上游尝试。
_Avoid_: 重试记录, 上游请求（当它被用来兼指逻辑请求时）

**429 账号上限**：
一次逻辑请求内允许因 429 耗尽同账号重试、需要切换的不同上游账号的最大数量；达到上限即停止为这次逻辑请求切换账号，后续逻辑请求独立计算。
_Avoid_: 会话冷却, API Key 冷却, 轮询次数（当它被用来兼指失败账号数时）

**使用记录**：
一次已计量调用的计费与用量事实，不是请求审计记录。
_Avoid_: 完整日志, 请求日志, usage 详情（当它被用来兼指审计时）

**未采集**：
该入口或阶段不在当前采集范围内。未采集不是失败，也不能用空字段冒充已采集的上游尝试。
_Avoid_: 缺失, 失败, 无日志

## 倍率声明

**对外倍率快照**：
本实例针对已认证下游密钥制作并在有效期内重复提供的倍率声明；它不冻结实际计费规则，也不是从本实例的上游获取的价格。
_Avoid_: 实时倍率, 上游价格, 计费锁定, 伪造倍率

## 请求审计

**请求审计记录**：
挂在使用记录上的只读链路：逻辑请求、其上游尝试、协议元数据与事件骨架。第一阶段只在已有使用记录时存在。
_Avoid_: 完整日志, 请求快照, 原始抓包, usage 正文

**协议元数据**：
路由、允许记录的头、协议字段名与枚举、体量、内容指纹，以及真实存在的会话或请求标识。它不是模型正文。
_Avoid_: 请求体, payload, 原始正文

**事件骨架**：
流式响应中每条事件的类型、序号、体量、内容指纹和完整性。它不含事件文本或增量。
_Avoid_: SSE 全文, delta, 流式正文

**模型正文**：
用户输入、模型输出、工具参数与结果、流式文本或增量。请求审计记录不保存模型正文。
_Avoid_: Token 内容（当它被同时用来指认证凭据和模型正文时）, prompt, completion

**内容指纹**：
对请求或事件做的带密钥摘要，用来关联重试或去重，不能还原模型正文，也不能跨租户关联。
_Avoid_: SHA256, 明文 hash, 指纹（当它被用来指 TLS 或伪造的客户端身份时）

**采集完整性**：
一条审计链路当前是完整、截断、未采集、采集失败、已脱敏、写入失败还是响应中途不完整。
_Avoid_: 成功, 有日志, 完整（当它被用来指保存了模型正文时）

**普通审计模式**：
审计写入失败时仍允许继续访问上游，并标记写入失败或不完整。
_Avoid_: 尽力审计（当它被当成强制审计时）

**强制审计模式**：
由超级管理员按路由或租户开启。上游发出前若逻辑请求或本次尝试的协议元数据无法写入，则阻断该次发出；响应已经开始后不能回滚，只能标为不完整。
_Avoid_: 全局 fail-closed, 账号级强制审计

## 账号可见授权

**账号查看资格**：
管理员按普通用户显式开启的附加只读能力；用户原有 API Key、模型调用与个人用量权限不变。资格本身不授予查看任何账号。
_Avoid_: 只读角色, 管理员只读权限, 分组授权

**可见账号分配**：
管理员将具体上游账号逐一分配给具有查看资格的普通用户。没有分配则可见集为空；分配不是分组或调度路由关系。
_Avoid_: 可用账号, 路由账号, 默认账号池

**已分配账号只读视图**：
普通用户当前可以查看的、仍未被手动禁用的已分配账号的基础资料和脱敏上游身份；不是管理员账号资料或账号导出。
_Avoid_: 管理员账号页, 账号凭据, 完整账号信息

## Claude 设置

**Claude 专属设置**：
Anthropic 路径上的一组能力，按项标记为保持现有、第一阶段做或待评估。截图或参考项目里的控件不等于本系统已承诺的行为。
_Avoid_: Claude 设置页, 官方客户端设置, 截图功能

**Claude Code 限制**：
现有的分组级限制：只接受被识别为 Claude Code 的请求，或降级到指定分组。它不是「非 Claude Code 一律拒绝」。
_Avoid_: 严格 CC 校验（当它被用来指参考项目里伪装时丢掉调用方 system 的 strict-mode 时）

**Claude Code 伪装**：
把非 Claude Code 请求改写成官方 Claude Code 形态。现有伪装保持不变；截图中的新伪装行为待评估，不作为已承诺能力。
_Avoid_: 宽松模式, cloak, 官方客户端兼容

**Thinking 禁用形态**：
`thinking.type` 为 `disabled` 时，该对象内只允许 `type`。默认去掉多余字段后转发；分组可改为按上游错误格式本地拒绝。
_Avoid_: thinking 校验（当它被用来指签名校验或 7 天拒绝缓存时）
